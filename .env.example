# Frontend Environment Variables
# ─────────────────────────────────────────────────────────────────────────────

# The base URL for the API. 
# On Vercel, if you route /api to the server, keep this as "/api".
# For local development with a separate server, use "http://localhost:3001/api".
VITE_API_BASE_URL=/api
